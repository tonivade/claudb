
jar {
  manifest {
    attributes('Automatic-Module-Name': 'com.github.tonivade.claudb.junit5')
  }
}

dependencies {
  api projects.claudbLib
  api libs.junitJupiter
  api libs.junitRunner
  testImplementation libs.jedis
  testRuntimeOnly libs.logbackClassic
}
